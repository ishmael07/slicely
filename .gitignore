node_modules/
dist/
dist-web/
.env
*.log
.DS_Store
out/
release/
*.gcode
*.bgcode

# Runtime artifacts (uploaded models, downloads, generated slices/configs, local settings)
uploads/
downloads/
slices/
configs/
settings.json
# Per-visitor web session workspaces (SLICELY_WORKDIR defaults to ~/Slicely,
# which on a case-insensitive filesystem IS this repo). Everything under here is
# runtime state. It was only invisible to git by accident — each file happened to
# match a pattern above — so name it explicitly.
sessions/
printers.json
printer-secrets.json
jobs.json

# Secrets & per-machine state (never commit)
.session-secret
master.key
printer-secrets.json
secrets.json

# a VM deployment's secrets live next to its compose file
deploy/oracle/.env
